package com.yoimerdr.compose.ludens.core.infrastructure.platform

/**
 * Marker interface for platform-specific context.
 *
 * Represents platform-specific context information required by the application.
 * Each platform (Android, iOS) provides its own implementation containing
 * the necessary context data for that platform.
 */
interface PlatformContext