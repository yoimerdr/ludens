# Ludens settings
# Read by build-logic in `LudensSettingsConfiguration` and
# converted into the generated `LudensSettingsPreset.kt`.

# ----- Settings -----

# Selects the default generated preset used by the app build.
# If this is `custom`, the values below are read instead of the built-in preset.
ludens.settings.presetName=recommended

# Preset raw values used when `ludens.settings.presetName=custom`.
# Boolean values accept `true/false`, `1/0`, `yes/no` and `on/off`.

# Default mute state for the tools overlay.
ludens.settings.preset.toolMuted=false

# Default visibility for the FPS counter.
ludens.settings.preset.toolShowFps=false

# Default WebGL switch state.
ludens.settings.preset.toolUseWebgl=false

# Default state for the virtual controls.
ludens.settings.preset.controlEnabled=true

# Virtual control opacity. Valid range: `0.0` to `1.0`.
# Used by the generated settings preset and the runtime UI.
ludens.settings.preset.controlAlpha=0.4

# Default state for the quick actions panel.
# When enabled, the generated preset exposes the configured action items.
ludens.settings.preset.actionEnabled=false

# Quick action items shown in the quick actions panel.
# Separator rules: comma, pipe or semicolon.
# Valid values currently used by the app: `settings`, `controls`, `mute`, `fps`, `webgl`.
ludens.settings.preset.actionItems=settings

# Default system theme fallback.
# Consumed by the runtime settings flow when the app has no explicit theme override.
ludens.settings.preset.systemTheme=system

# Default system language fallback.
# Consumed by the runtime settings flow when the app has no explicit language override.
ludens.settings.preset.systemLanguage=system

# ----- Languages -----

# Which languages to include in the build.
# "*" means all discovered from assets/languages/.
# Uncomment and list only the tags you need to limit the build.
# ludens.languages.available=en,es

# ----- Fonts -----

# Base fonts (always included, change only if you customize the .ttf files).
# ludens.fonts.display=plusjakartasans.ttf
# ludens.fonts.body=inter.ttf

# Language-specific font fallbacks.
ludens.fonts.language.ja.body=notosans_sc.ttf
ludens.fonts.language.ja.display=notosans_sc.ttf
ludens.fonts.language.zh.body=notosans_sc.ttf
ludens.fonts.language.zh.display=notosans_sc.ttf

# ----- App Identity -----

# Shared cross-platform application identity, read by `LudensAppConfiguration`.
# Both Android and iOS use these values by default. Override a single value for one
# platform only via the `ludens.android.*` / `ludens.ios.*` keys further below — leave
# it unset there to keep using the shared value here.

# Shared reverse-DNS application id (letters, digits and dots only — no underscores or
# hyphens, so the same value is always valid as both an Android application id and an
# Apple bundle identifier).
ludens.app.id=com.ludens.compose.ludens

# Shared display name shown by both platforms.
ludens.app.name=Ludens

# Shared version string (dot-separated integers, e.g. 0.4.0).
# This is the visible version, not the integer version code below.
ludens.app.version=0.4.0

# Shared version code / build number.
# Used by Android for update compatibility and by iOS as CURRENT_PROJECT_VERSION.
# Should be incremented with every release; does not have to match the version string.
ludens.app.versionCode=1

# ----- Android Identity -----

# Android-only identity settings, read by `LudensAndroidConfiguration` and applied by
# `composeApp/build.gradle.kts`.

# Per-platform overrides of the shared `ludens.app.*` values above.
# Leave commented out to use the shared value for Android; uncomment only when Android
# specifically needs to diverge (e.g. a different package id per store).
# ludens.android.id=
# ludens.android.version=
# ludens.android.versionCode=
# ludens.android.name=

# Launcher label shown below the icon (Android-only, no shared equivalent).
ludens.android.launcherName=Ludens

# Minimum Android API level supported.
ludens.android.minSDK=21

# Target Android API level for the build.
ludens.android.targetSDK=36

# Application immersive mode
ludens.android.immersive=true

# ----- Android Manifest -----

# Android manifest defaults.
# These values are consumed by the manifest generation / placeholder flow.

# Whether Android can back up app data.
# Maps to `android:allowBackup`.
ludens.android.manifest.allowBackup=true

# Requests a larger heap for the process.
# Maps to `android:largeHeap`.
ludens.android.manifest.largeHeap=true

# Enables hardware acceleration on the application window.
# Maps to `android:hardwareAccelerated`.
ludens.android.manifest.hardwareAccelerated=true

# Activity orientation used by the app entry point.
# Maps to `android:screenOrientation`.
# Common values: `sensorLandscape`, `landscape`, `portrait`, `fullSensor`.
ludens.android.manifest.screenOrientation=sensorLandscape

# Allows cleartext HTTP traffic.
# Maps to `android:usesCleartextTraffic`.
# Keep this disabled unless the game or web assets require HTTP.
ludens.android.manifest.usesCleartextTraffic=false

# Allows the activity to be resized by the system.
# Maps to `android:resizeableActivity`.
ludens.android.manifest.resizeableActivity=false

# ----- Android Permissions -----

# Android permissions defaults.
# These are read by the permissions manifest generator.

# Grants network access.
# Maps to `android.permission.INTERNET`.
# Usually required for Ludens because the game is loaded through WebView.
ludens.android.permissions.internet=false

# Grants access to connectivity state.
# Maps to `android.permission.ACCESS_NETWORK_STATE`.
ludens.android.permissions.networkState=false

# Lets the app keep the CPU awake while rendering or loading.
# Maps to `android.permission.WAKE_LOCK`.
ludens.android.permissions.wakeLock=false

# Grants access to Wi-Fi connection state.
# Maps to `android.permission.ACCESS_WIFI_STATE`.
ludens.android.permissions.accessWifiState=false

# Grants permission to change Wi-Fi state.
# Maps to `android.permission.CHANGE_WIFI_STATE`.
ludens.android.permissions.changeWifiState=false


# ----- App Icon Generator -----

# Name of the source master icon in project/assets/icons/
ludens.icons.name=icon

# Name of the foreground adaptive layer in project/assets/icons/
ludens.icons.foreground=icon_foreground

# Solid hex color background or resource reference
ludens.icons.background=#FDFDFD

# Android-specific configuration
ludens.icons.android.enable=true
ludens.icons.android.format=webp
ludens.icons.android.playstore=true

# iOS-specific configuration
ludens.icons.ios.enable=true

# Scale of the foreground asset inside the adaptive icon viewport
ludens.icons.scale=0.62

# ----- Android Build Post-Processing -----

# Enables post-build APK and AAB rename and relocate task
ludens.android.build.enable=true

# Destination directory relative to the root project (placeholders like {buildType} or {variant} supported)
ludens.android.build.outputDir=output/{buildType}

# Base template pattern for artifact renaming (file extension is auto-appended)
# Placeholders: {appName}, {name}, {versionName}, {versionCode}, {buildType}, {minSdk}, {targetSdk}, {timestamp}, {appId}, {artifactType}
ludens.android.build.pattern={appName}-{versionName}-{buildType}

# Operation mode: "copy" (duplicate into destination) or "move" (relocate)
ludens.android.build.action=copy

# Filter variants: "all", "release", "debug" or comma-separated list
ludens.android.build.includeVariants=all

# ----- iOS Identity -----

# Per-platform overrides of the shared `ludens.app.*` values above.
# Leave commented out to use the shared value for iOS; uncomment only when Apple's tooling
# needs a value the shared default can't satisfy (e.g. `ludens.app.id` isn't a valid choice
# for this app's bundle identifier for some reason).
# ludens.ios.bundleId=
# ludens.ios.version=
# ludens.ios.versionCode=
# ludens.ios.name=

# ----- iOS Build Pre-Processing -----

# Enables iOS pre-build metadata sync (PRODUCT_NAME, PRODUCT_BUNDLE_IDENTIFIER, MARKETING_VERSION, CURRENT_PROJECT_VERSION)
# Mirrors ludens.android.build.enable. The Kotlin-side default in PreBuildExtension stays false.
# Maps to `ludens.ios.build.enable`.
# This feature has not been tested in any way
ludens.ios.build.enable=false

# ----- Debug & Error Reporting -----
# Enable rich error interception and show a detailed traceback Dialog for runtime errors.
# Only available when YDP_Ludens 1.2.0+ is used in the rpg maker project.
ludens.debug.errors=true


