<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="${ludensAllowBackup}"
        android:enableOnBackInvokedCallback="true"
        android:hardwareAccelerated="${ludensHardwareAccelerated}"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="${ludensLargeHeap}"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="${ludensUsesCleartextTraffic}"
        tools:targetApi="33">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true"
            android:label="@string/app_launcher_name"
            android:resizeableActivity="${ludensResizeableActivity}"
            android:screenOrientation="${ludensScreenOrientation}">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
